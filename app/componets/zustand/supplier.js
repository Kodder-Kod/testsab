 import { create } from 'zustand'


export const useUserSupplier = create((set) => ({
    userSupplier: '',
  }))
  
  
  export const useUserSupplierTotal = create((set) => ({
    userSupplierTotal: '',
  }))
  
  
  export const useUserSupplierData = create((set) => ({
    userSupplierData: '',
  }))
