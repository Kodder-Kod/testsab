 import { create } from 'zustand'


export const useUserRoom = create((set) => ({
    userRoom: '',
  }))
  
  
  export const useUserRoomTotal = create((set) => ({
    userRoomTotal: '',
  }))
  
  
  export const useUserRoomData = create((set) => ({
    userRoomData: '',
  }))
